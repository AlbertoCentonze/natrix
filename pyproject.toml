[project]
name = "natrix"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click>=8.1.7",
    "vyper==0.4.0",
]
[tool.uv]
dev-dependencies = [
    "pytest>=8.3.3"
]
